runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Real-time US court records search for AI agents. Query by name, case number, or attorney. Returns case status, filings, summaries."
